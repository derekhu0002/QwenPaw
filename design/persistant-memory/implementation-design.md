# Implementation Design Memory

- 2026-06-08: Audited `sec-e2e-025-audit-integrity-self-healing-lockdown` after intent clarified historical audit-record tamper. Repository evidence showed the prior explicit entrypoint drove one high-risk baseline and mutated latest/checkpoint evidence, so it did not prove the required branch: three high-risk audit records, OS-level edit of the second committed non-tail record, then another high-risk attempt.
- Updated the frozen explicit acceptance design in `tests/integration/security/test_audit_foundation.py` and protected harness vocabulary in `tests/integration/security/harness.py`. The control point now creates three high-risk records, edits the second committed non-tail record, and attempts another high-risk command; the observation point requires historical-record tamper detection, `UNTRUSTED` lockdown, high-risk denial, and Security Center backend/web recovery-required or `UNTRUSTED` rather than `CLEAR`.
- Updated implementation contracts at `OVERALL_ARCHITECTURE.md`, `src/qwenpaw/security/ARCHITECTURE.md`, `deploy/api/ARCHITECTURE.md`, `deploy/web/ARCHITECTURE.md`, `tests/ARCHITECTURE.md`, and `tests/integration/security/ARCHITECTURE.md` to freeze whole-ledger verification and no-premature-CLEAR behavior.
- Updated `design/KG/ImplementationToCodingHandoff.json` and `design/KG/test-failure-records.json` so @coding-and-repairing owns the expected failing entrypoint `tests/integration/security/test_audit_foundation.py::test_audit_integrity_self_healing_lockdown` without editing the frozen test or harness.
- Validation evidence: focused pytest for `tests/integration/security/test_audit_foundation.py::test_audit_integrity_self_healing_lockdown` failed as expected with `Historical_Record_Tamper_Not_Detected`; `npm run validate:system-architecture`, `npm run validate:handoff:implementation`, MCP `validateStageHandoff(stage="implementation-to-coding")`, and `node tests/architecture/security-explicit-entrypoint-traceability.test.js` passed. Full `npm run test:argo` reported 3/4 security explicit entrypoints passed and only sec-e2e-025 failed.
- 2026-06-09: Audited Coding/Repair delivery for `sec-e2e-025-audit-integrity-self-healing-lockdown`. Production repair was limited to `src/qwenpaw/security/audit_foundation.py` plus coding memory, while frozen acceptance files kept the strengthened three-record/second-non-tail tamper contract. Fresh verification showed `.venv` focused sec-e2e-025 passed and `npm run test:argo` passed 4/4 security explicit entrypoints. Updated `design/KG/ImplementationToCodingHandoff.json` and `tests/architecture/security-explicit-entrypoint-traceability.test.js` to remove the stale sec-e2e-025 expected-failure/coding-target state now that `design/KG/test-failure-records.json` is empty.
- 2026-06-09: Audited remaining `sec-e2e-027-lease-expiry-active-defense` after intent clarified the boundary as pre-proof `UNTRUSTED/423` block followed by post-proof `ALIGNED` or `TRUSTED` plus model-access restore for the same canonical client. Confirmed `tests/integration/security/test_audit_foundation.py::test_lease_expiry_blocks_untrusted_rejoin_until_gap_sync` and `tests/integration/security/harness.py` are complete frozen business-readable entrypoints with GIVEN/WHEN/THEN, harness abstraction, semantic names, two control points, two console observations, and `Lease_Expiry_Active_Defense_Gap` failure categories.
- Updated `design/KG/ImplementationToCodingHandoff.json` and `design/KG/test-failure-records.json` without touching production implementation. Validation evidence was mixed: direct focused `.venv` pytest for sec-e2e-027 passed once, but MCP `runArchitectureTests` refreshed `design/KG/test-failure-records.json` with `UNTRUSTED_Lease_Downgrade_Missing`, `Recovery_Control_Point_Missing`, and `Post_Recovery_Model_Access_Restore_Missing` plus `runtime_console_error=timeout: timed out`. Treat sec-e2e-027 as not closed for Coding/Repair until the frozen entrypoint is deterministic under the Argo explicit gate.
- 2026-06-09: Audited Coding/Repair delivery for `sec-e2e-027-lease-expiry-active-defense`. Production changes were under `deploy/api/store.py`, `src/qwenpaw/security/audit_foundation.py`, and `src/qwenpaw/app/routers/console.py`; frozen `tests/integration/security/test_audit_foundation.py` and `tests/integration/security/harness.py` still preserve the recovery control point, pre/post console observations, and business failure categories. Verification evidence: focused sec-e2e-027 pytest passed, full 9-entry explicit pytest passed, `npm run test:argo` passed 4/4, MCP `runArchitectureTests` passed 4/4 with `failureRecords=[]`. Updated `design/KG/ImplementationToCodingHandoff.json` and `tests/architecture/security-explicit-entrypoint-traceability.test.js` to mark sec-e2e-027 resolved and prevent stale sec-e2e-027 coding targets or failure records.
- 2026-06-09: Materialized `sec-e2e-028-normal-offline-reconnect-clear-state` without modifying production implementation. Added frozen entrypoint `tests/integration/security/test_audit_foundation.py::test_normal_offline_reconnect_clears_without_gap_recovery`, protected harness vocabulary in `tests/integration/security/harness.py`, and test-fixture cleanup in `tests/integration/conftest.py` so normal offline restart can be exercised through the real app subprocess. Updated `OVERALL_ARCHITECTURE.md`, `src/qwenpaw/security/ARCHITECTURE.md`, `deploy/api/ARCHITECTURE.md`, `deploy/web/ARCHITECTURE.md`, `tests/ARCHITECTURE.md`, `tests/integration/security/ARCHITECTURE.md`, `tests/architecture/security-explicit-entrypoint-traceability.test.js`, `design/KG/ImplementationToCodingHandoff.json`, and `design/KG/test-failure-records.json`. Verification: py_compile passed, lints reported no issues, `npm run validate:system-architecture` passed, `npm run validate:handoff:implementation` passed, `node tests/architecture/security-explicit-entrypoint-traceability.test.js` passed, and MCP `validateStageHandoff(stage="implementation-to-coding")` passed. Focused sec-e2e-028 currently remains failed/unstable: first concrete pytest failed around normal restart readiness before restored-access observation, and the adjusted rerun exceeded 300s without stable pytest output; Coding/Repair must repair production normal reconnect CLEAR projection while keeping frozen test/harness unchanged.
- 2026-06-09: Audited Coding/Repair delivery for `sec-e2e-028-normal-offline-reconnect-clear-state`. Production repair stayed under `deploy/api/store.py`, `src/qwenpaw/security/audit_foundation.py`, and `src/qwenpaw/app/routers/console.py`; frozen explicit entrypoint and protected harness still preserve baseline registration, trusted audit head, normal offline control point, restart-before-lease-expiry, backend/web CLEAR projection, ordinary model access 200, and no gap-validation false positive. Supporting guard `tests/contract/security/test_lease_recovery_semantics_contract.py::test_security_center_keeps_same_head_normal_reconnect_clear` now covers same-head in-window reconnect semantics and lease TTL non-shrinking without replacing the explicit entrypoint. Fresh verification passed: focused contract, full `tests/integration/security/test_audit_foundation.py` 5/5, `npm run test:argo` 5/5 with empty failure records. Updated `design/KG/ImplementationToCodingHandoff.json` and `tests/architecture/security-explicit-entrypoint-traceability.test.js` so sec-e2e-028 is recorded as resolved and ready for final intention-design audit.
- 2026-06-09: Audited the second Coding/Repair delivery for `sec-e2e-028` Argo timeout stability. The `src/qwenpaw/app/_app.py` change skips only foreground active-agent preload when Security Center is configured and `audit_chain_checkpoint.json` already exists, letting `/api/version` readiness and runtime heartbeat recover before expensive background agent startup; this does not move security semantics out of `src/qwenpaw/security` and keeps first bootstrap behavior intact. Focused `tests/integration/security/test_audit_foundation.py::test_normal_offline_reconnect_clears_without_gap_recovery` passed in 84.34s, and Argo reported sec-e2e-028 passed in 114950ms, but full `npm run test:argo` failed 4/5 because `sec-e2e-027-lease-expiry-active-defense` reopened with `Model_Access_Reconnect_Gate_Missing` and `Post_Recovery_Model_Access_Restore_Missing`. Updated `design/KG/ImplementationToCodingHandoff.json` and `tests/architecture/security-explicit-entrypoint-traceability.test.js` to record sec-e2e-028 as stable but block final intention-design audit until Coding/Repair restores sec-e2e-027 and clears `design/KG/test-failure-records.json`.
- 2026-06-09: Audited Coding/Repair rework for the reopened `sec-e2e-027` regression plus `sec-e2e-028` stability. Production changes stayed under `src/qwenpaw/security/audit_foundation.py`, `src/qwenpaw/app/_app.py`, `src/qwenpaw/cli/app_cmd.py`, and `deploy/api/app.py`; frozen `tests/integration/security/test_audit_foundation.py`, `tests/integration/security/harness.py`, and `tests/integration/conftest.py` remained untouched for this delivery. Architecture judgment: lightweight unauthenticated integration-mode Security Center startup and short graceful shutdown budgets are acceptable implementation scheduling under the existing runtime/deploy boundaries because security semantics still live in `src/qwenpaw/security` and deploy/api; no ARCHITECTURE.md change was required. Fresh verification passed: focused sec-e2e-027 in 39.40s, focused sec-e2e-028 in 58.59s, full `tests/integration/security/test_audit_foundation.py` 5/5 in 74.06s, and `npm run test:argo` 5/5 with sec-e2e-027 duration 68921ms and sec-e2e-028 duration 85525ms. Updated `design/KG/ImplementationToCodingHandoff.json` and `tests/architecture/security-explicit-entrypoint-traceability.test.js` back to resolved state; `design/KG/test-failure-records.json` is empty and final intention-design audit may proceed.
