# Coding And Repair Memory

- 2026-06-09: Repaired `sec-e2e-025-audit-integrity-self-healing-lockdown` without modifying frozen tests. Root cause was that `src/qwenpaw/security/audit_foundation.py` preflight trusted the latest trace/checkpoint and did not recompute the committed trace ledger, so an OS edit of the second committed non-tail audit record could evade detection until later projections. Added whole-ledger verification over committed trace records, high-risk preflight lockdown on ledger tamper, checkpoint revocation after confirmed ledger tamper, and lockdown evidence fields for historical/non-tail tamper, fork point, and recovery-required projection. Verified with focused pytest and argo-validator full explicit security tests.
- 2026-06-09: Repaired `sec-e2e-027-lease-expiry-active-defense` / `Lease_Expiry_Active_Defense_Gap` without modifying frozen tests or harnesses. Root causes were cloud recovery release not refreshing the same canonical client's lease timing, restored model access not writing the cloud-validated trusted anchor back into the local checkpoint before later heartbeats, and background heartbeat auto-proofing a denied `AUDIT_INTEGRITY_LOCKDOWN` frame before the explicit recovery control point. Fixed `deploy/api/store.py` so an already-open recovery gate cannot close without accepted gap validation and successful recovery release renews the lease; fixed `src/qwenpaw/security/audit_foundation.py` so restored access persists the recovered trusted anchor and passive heartbeat proof only applies to lease-expiry frames, not model-access lockdown frames. Verified focused `sec-e2e-027`, full 9-entry explicit pytest, `npm run test:argo`, and MCP `runArchitectureTests` all pass.
- 2026-06-09: Stabilized `sec-e2e-027-lease-expiry-active-defense` and the Argo explicit security gate without modifying `tests/integration/security/test_audit_foundation.py` or `tests/integration/security/harness.py`. Root causes were: Security Center accepted full-chain gap proof only while a recovery gate was already open, so post-recovery chain extension could be misclassified as `local_hash_mismatch`; restored model access synchronously waited for an extra heartbeat and could exceed the harness stream read timeout; and the background heartbeat emitter wrote passive recovery proof traces for non-lease lockdown/tamper states, racing the frozen latest-trace observation for `sec-e2e-025`. Updated `deploy/api/store.py` to accept validated full-chain gap proof as a trusted-head advance even after gate closure, restricted passive reconnect proof in `src/qwenpaw/security/audit_foundation.py` to true `lease_ttl_expired` recovery only, and removed the restored-access path's synchronous heartbeat wait in `src/qwenpaw/app/routers/console.py`. Verified standalone `sec-e2e-025`, standalone `sec-e2e-027`, full 9-entry explicit pytest, `npm run test:argo`, and argo-validator MCP `runArchitectureTests` all pass.
